# Як тут перекладають

Робочий довідник: правила, усталені рішення й пастки, на які вже наступали.
Мета документа — щоб наступний захід не починався з дослідження заново.

Стан і цифри — у [PROGRESS.md](PROGRESS.md). Тут — **як**, а не **скільки**.

---

## 1. Команди

| Команда | Що робить |
|---|---|
| `py tools\loc.py` | валідація (cp1251 + гліфи + абетки) → усі композитори → `build.py` → перевірка `esmtool` |
| `py tools\glyphs.py` | які символи шрифт узагалі вміє намалювати |
| `py tools\loc.py save` | `git add/commit/push`; текст коміту береться з `tools\_msg.txt` |
| `py tools\loc.py all` | збірка, а тоді коміт |
| `py tools\q.py dup` | **один англійський рядок — два переклади?** Має бути `0` |
| `py tools\q.py key <підрядок>` | як ми вже перекладали ключі з цим підрядком |
| `py tools\q.py val <підрядок>` | де в перекладах трапляється цей рядок |
| `py tools\q.py left <категорія> [N]` | що лишилося неперекладеним |
| `py tools\items\settle_names.py` | суперечка «одна назва — два написання»: хто перемагає за корпусом; `--apply` переписує безпечні |
| `py tools\dialogue\latin_left.py` | англійські назви, що лишилися **всередині** вже перекладених реплік |
| `py tools\items\check_generated.py` | чи перезбірка не з'їла ручних правок у складених `uk_*.json` |
| `py tools\books\extract_books.py --apply` | зібрати книги з модліста, оновити метадані |
| `py tools\books\propagate.py --apply` | перенести переклад книги на її близнюків з інших плагінів |
| `py tools\topics\check_sources.py` | те, що ганяє CI: зрізи, теми, GMST, книги |
| `py tools\audit.py` | перерахувати обсяги виміряно, а не оцінкою |
| `py tools\items\check_gender.py` | рід професії на вивісці проти статі NPC (бере стать із гри) |

`loc.py` запускає git і esmtool підпроцесами **зсередини Python**, тож дозвіл
треба дати один раз на сам `py`, а не на кожну команду.

---

## 2. Головне правило: одна назва — один переклад

`patch_names.py` зливає **всі** `tools/items/uk_*.json` в **один** словник і
застосовує його до `FNAM` усіх типів записів одразу:

```
ARMO WEAP CLOT MISC INGR ALCH NPC_ CREA SPEL CONT DOOR ACTI
LIGH APPA LOCK PROB REPA CLAS FACT RACE BSGN REGN BOOK
```

Наслідок: якщо `Bound Dagger` перекладено і як предмет, і як чари, але
по-різному — **у гру потрапить випадковий із двох**. Тому після кожної партії:

```
py tools\q.py dup      # має бути «розбіжностей: 0»
```

За сесію так знайшлося 73 такі колізії. Це не дрібниця, це головний клас помилок.

### Теми діалогів теж рахуються

У `topics/uk_dial_topics*.json` ключ — це **номер рядка** в `dial_topics.json`,
а не англійська назва. Через це `dup` довго не бачив цілого пласту: теми
перекладали окремо від предметів, і та сама річ називалася двома словами.
Тепер `q.py corpus()` розгортає номер назад в англійську, і питання те саме,
що для предметів. Перший же запуск показав **319** суперечок:

| Англійська | у темі | у словнику |
|---|---|---|
| Bitter Cup | Гірка Чаша | Гіркий келих |
| Cloudcleaver | Хмарорубач | Хмароріз |
| Enamor | Енамор | Причара |
| Withershins | Навспак | Проти сонця |
| Gentleman Jim Stacey | Ґентлеман Джім Стацеі | Джентльмен Джим Стейсі |

**Хто правий — вирішує корпус, а не словник.** У словнику кожен варіант
трапляється один раз, тож рахувати нема чого; а репліки, журнал і книги —
це те, що гравець читає, і написання, яке там уже стоїть сотню разів, і є
усталеним. Це рахує `settle_names.py`; `--apply` чіпає лише ті, де програлий
варіант у корпусі не трапляється жодного разу.

Коли корпус помиляється більшістю, рішення записують у `SKIP` того ж скрипта
з поясненням — щоб наступний запуск не «виправив» його назад.

Де корпус мовчить, працюють правила: `--case` зводить різницю в регістрі
всередині назви (велика лишається першому слову й власним назвам), решту
розв'язує §4 і родина назв — `Bonelord Gravetender` пишеться як усі інші
`Bonelord`, а `Tusked Bristleback` — як усі `Bristleback`.

Три пари лишаються навмисно різними і внесені в `ALLOW` у `q.py`: назва раси
чи народу — це одна особа (**Імперець**, **Каджит**, **Скаал**), а тема
діалогу про той самий народ стоїть у множині (**імперці**, **каджити**,
**скаали**). Обидва написання правильні.

`CELL NAME` навмисно **не** патчиться (`--cells` вимкнено): назва комірки — це
ідентифікатор, на який посилаються скрипти й двері. Тому назви місць заблоковані.

---

## 3. Кодування й символи

Перевірок **три, і вони незалежні**. Символ мусить пройти всі:

1. **cp1251** — гра читає саме його. Чого в ньому нема, того нема взагалі:
   рядок зникає цілком.
2. **гліф у шрифті** — навіть те, що cp1251 кодує, може не намалюватися.
   Тоді в грі на місці символа порожнеча, а помилки ніде не видно.
3. **єдиний апостроф** — `’` проходить обидві попередні й усе одно заборонений,
   бо в корпусі має бути тільки ASCII `'`.

Друга перевірка з'явилася пізніше за першу, і саме вона зловила давню помилку:
питомо українські лапки-ялинки в cp1251 **є**, а гліфів у наших шрифтах **нема**.

| Символ | cp1251 | Гліф | Висновок |
|---|:---:|:---:|---|
| `'` ASCII | є | є | **єдиний дозволений апостроф** |
| `ʼ` U+02BC | **нема** | — | рядок зникне цілком |
| `’` U+2019, `‘` U+2018 | **є** (0x92, 0x91) | є | обидві перевірки проходять — і все одно **не вживати** |
| `“ ”` U+201C/201D | є | є | **робоча пара лапок** |
| `« »` | є | **нема** | порожнеча в грі |
| `„` | є | **нема** | порожнеча в грі |
| `—` `–` `…` `°` | є | є | вільно |
| `№` `§` `•` `·` | є/нема | **нема** | не вживати |
| `ґ Ґ є Є і І ї Ї` | є | є | шрифт пропатчено під них |

Рядок про `’` варто прочитати двічі. Він **не** ламає ні cp1251, ні шрифт —
раніше тут стояло протилежне, і це було просто неправдою. Заборонений він
з іншої причини: апостроф у корпусі мусить бути **один**, інакше в тому самому
реченні трапляються обидва. Тому це окрема, третя перевірка в `check_sources`,
яку не видно ні за кодуванням, ні за гліфами.

```
py tools\glyphs.py             # покриття розділових знаків
py tools\glyphs.py "текст"     # що саме з цього рядка не намалюється
py tools\glyphs.py --dump      # оновити font_coverage.json після патчу шрифта
```

Покриття беруть **перетином** усіх `Fonts/*.ttf`: інтерфейс і книги малюються
різними шрифтами. Самих `.ttf` у git нема (вони походять від гри), тому для CI
поруч лежить `tools/font_coverage.json` — так само, як `books_meta.json` біля
книг. Патчили шрифт — перезапустіть `--dump`.

`loc.py` **сам** виправляє те, для чого заміна однозначна (`glyphs.SUBST`:
ялинки й `„` → `“ ”`, будь-який апостроф → ASCII) і каже, скільки разів
довелося. Решту показує як помилку — це має побачити людина.

Латинську літеру всередині кириличного слова (`Нiвалiс` із латинським `i`)
він теж вважає помилкою: cp1251 таке пропускає мовчки, а в грі виходить зламане
слово. Мітки варіантів моделей (`Чорна01-1S`) дозволені: їх видно за цифрою.

---

## 4. Транслітерація

| Латиниця | Кирилиця | Приклад |
|---|---|---|
| `g` | **г** | Gnisis → Гнісіс, Gothren → Готрен, Dagoth → Дагот *(див. нижче)* |
| `h` | **г** | Hircine → Гірсін, Helseth → Гелсет |
| `kh` | **х** | Khuul → Хуул, Khajiit → Каджит |
| `c` перед `e i y` | **с/ц** (м'яко) | Tsaesci → Цаес**ці**, Cienne → Сієнне |
| `c` в інших місцях | **к** | Durgoc → Дурґо**к** |
| `th` | **т** | Boethiah → Боетія, Sarethi → Сареті |
| `sh`, `ch` | **ш**, **ч** | Sheogorath → Шеоґорат, Kvatch → Кватч |
| `-yon` | **-йон** | Aryon → Арйон, Andvaryon → Андварйон |
| `ye` | **є** | Yessur → Єссур, Dirvanye → Дірванє |
| `y-` на початку | **і** | Yngling → Інґлінґ *(українське слово з «и» не починається)* |
| латинське `-ius` | **-ій** | Falvius → Фалвій, Rosunius → Розуній |
| `J-` каджитське | **Дж** | J'Zhirr → Дж'Жірр |
| `J-` нордське | **Й** | Jormar → Йормар |
| `Hl-` | **Хл-** | Hlaalu → Хлаалу *(див. §5)*; тепер і в двигуні, інакше 33 імені виходили «Глеран» |

**Про `g`.** Довго тут стояло «ґ», і словники предметів так і складено: 254
назви з `G` починаються на Ґ проти 89 на Г. Але корпус — те, що гравець читає —
каже інакше: серед тих самих назв **609 входжень із Г проти 79 з Ґ** (Гнісіс
205, Готрен 59, Галбедір 24, Гілдан, Глатель, Гнаар Мок…). Так само й
правопис: `g` у власних назвах передаємо через **г**, а ґ лишається окремим
випадкам. Тому суперечки зводимо до **г**.

Тепер це правило живе не лише тут, а й у самому `translit.py`: доти таблиця
двигуна давала ґ, і кожна перезбірка тягла словник назад, хай що написано в
цьому файлі. Заразом зведено **494 імені**, які раніше лишалися з Ґ просто
тому, що в репліках не траплялися.

**Два винятки, і обидва за голосом корпусу.** Правило «зводимо до г» — це
тай-брейк для випадків без свідчень; там, де корпус висловився ясно, виграє
корпус, інакше словник почне сперечатися з тридцятьма репліками:

| Ім'я | Корпус | Рішення |
|---|---|---|
| Yngling | Інґлінґ 29 : Інглінг 2 | **Інґлінґ** — ґ лишається |
| Sheogorath | Шеогорат 45 : Шеоґорат 7 | **Шеогорат** — сім словникових зведено |

Орківську частку `gro-/gra-` не чіпано: корпус щодо неї розділений майже навпіл
(ґро 122 : гро 73, ґра 65 : гра 51), і це окреме питання.

**Власні назви транслітеруємо, описове перекладаємо.** «Hagrad the Stone» — це
Гаґрад Камінь, а не Гаґрад Стоне. Пастка в тому, що недогляд помітний лише в
словнику: «Skink-in-Tree's-Shade» сім разів стоїть у щоденнику як
«Скінк-у-Тіні-Дерева», а в словнику імен лежав «Скінк-Ін-Трі'С-Шаде» — і
`dup` цього не бачив, бо англійський ключ один.

Це правило довго діяло лише там, куди дивилися очима: у `translit.py` бракувало
близько сотні прізвиськ, і виходило «Хлоґґар Блуді», «Тарстен Геарт-Фанґ»,
«Свімс-Ін-Свеллс». Тепер словник `WORDS` покриває три родини:

| Взірець | Приклад | Стало |
|---|---|---|
| норди й імперці, `X the Y` | Sorkvild the Raven | Сорквілд **Крук** |
| норди, складанка з дефісом | Tharsten Heart-Fang | Тарстен **Серце-Ікло** |
| аргоніани, описове ім'я | Swims-In-Swells | **Плаває-У-Хвилях** |

**Що НЕ перекладаємо:** аргонійські та ешлендерські імена з дефісом, які
англійських слів не містять (`Ashur-Dan`, `Chanil-Lee`, `Sul-Matuul`,
`Heem-La`), і орківські патроніми `gro-`/`gra-`. Це власні назви.

Двох речей плаский словник зробити не може, тож вони йдуть у
`npc_overrides.json` вручну:

* **рід** — «Erna the Quiet» це Ерна **Тиха**, а не «Тихий». Іменникові
  складанки («Снігова-Пісня», «Довга-Нога») роду не потребують і годяться всім;
* **`X of Y`** — словник викидає `of`, бо для «Aengoth the Jeweler» це слушно.
  А «Mollimo of Cloudrest» потребує прийменника й родового: Моллімо
  **з Клаудресту**.

---

## 5. Усталені рішення зі змістом

Ці вибори вже зроблені й **не переглядаються без причини** — інакше знову
розповзеться. Причина записана, щоб було видно, чому саме так.

### Лор і власні назви

| Англійською | Українською | Чому саме так |
|---|---|---|
| Hlaalu | **Хлаалу** | 47 входжень проти 12 «Глаалу»; кластер `Хл` читається природніше |
| The Reach | **Обшир** (край) | «Предел» — калька з російської; обшир — питоме слово для розлогого краю |
| Reachman | **ричмен** | народ лишається екзонімом, як норди чи бретони |
| Welkynd Stone | **камінь велкінд** | welkynd — айлейдське «небо», не топонім; «велкіндський» вигадав би неіснуюче місце |
| Varla Stone | **камінь варла** | те саме: varla — «зоря» |
| Sanguine (речі) | **Санґвінів/-ова/-ові** | набір названо за даедричним князем, а не за кров'ю; «кривавий» — хибний слід за латинським *sanguineus* |
| Bound X (чари) | **Викликаний X** | спорядження прикликається на час; «зв'язаний» — калька |
| Lich | **Ліч** | не «Ліх» |
| Tear (місто) | **Тір** | це данмерське місто, а не сльоза |
| Mehrunes Dagon | **Мерунес Дагон** | тут `g` дає **г**, усталено в предметах |
| Molag Bal | **Молаг Бал** | тут теж **г**. У цьому рядку раніше стояло «Молаґ» — рішення ухвалили, дивлячись лише на словник активаторів (5 записів) і не побачивши 36 «Молаг Бал» у діалогах |
| House of Troubles | **Дім Бід** | так у щоденнику завдань, 9 разів («Четвертий Кут Дому Бід — Шеогорат») — саме це гравець читає в журналі |
| Pelagiad | **Пелагіад** | через `г`, як у ключах від форту |
| East Empire Company | **Східна Імперська Компанія** (СІК) | скорочення СІК уже в двох десятках назв книг |
| Census and Excise | **канцелярія перепису й мита** | усталено на вивісці |
| Miner | **шахтар** | звичайне українське слово, не «копач» |
| Spell Breaker | **Чаролам** | коротше й природніше за «Заклинаннєлам» |
| Ashlander | **ешлендер** | 45 входжень проти 4; і головне — тема діалогу 40 зветься «Ешлендери», саме по ній гравець клікає |
| Rogue Ashlander | **ешлендер-відступник** | так у темі 2154 |
| Caldera | **Кальдера** | «Кальдерська шахта», «Гільдія Магів у Кальдері» — 8 проти 4 |
| Vault | **сховище** | так у 12 назвах; «скарбниця» була випадковим синонімом |
| Septim (монета) | **септим** | а сам рід — Септіми, як у темі 179 «Тайбер Септім» |
| drake (монета) | **дрейк** | розмовна назва септима |
| Ghostgate | **Брама Привидів** | 95 входжень у корпусі проти 2; Ghostfence лишається «Примарною Огорожею» |
| Wraithguard, Sunder, Keening | **Примароварт, Розкол, Голосіння** | три артефакти одного коваля названі за одним правилом: англійське складне слово перекладаємо (як Хмароріз, Золотоклинок, Черепотрощник), а не транслітеруємо |
| Ash-* | **попеловий** | попелова буря, попелові вампіри, попеловий ямс, попеловий шанкр — уся родина одним словом |
| Eydis Fire-Eye | **Ейдіс Вогняне Око** | так її звати в грі; «Вогнеока» жила тільки в тексті |
| Caius Cosades | **Кай Косадес** | `s` між голосними лишається «с» |
| Grandmaster | **грандмайстер** | і ранг, і тір алхімічного реманенту |
| Auriel | **Аурієль** | «е» після «і» дає «є»: спанієль, Габрієль |
| Nchuleft | **Нчулефта** (род.) | як назва поселення |
| Cyrodiil | **Сиродіїл** | так зветься тема діалогу 229; і за правописом два «і» поспіль дають «ії» |
| fetcher (лайка) | **паскудник** | звичайна українська лайка того самого рівня |
| Hortator | **Хортатор** | 285 входжень проти 29 «Гортатор»; те саме `H → Х`, що в Хлаалу |
| Julianos | **Юліанос** | так у семи святинях і чарах; не «Джуліанос» |
| High Rock | **Хай Рок** | 10 входжень у темах; решта провінцій теж транслітеровані (Гаммерфел, Саммерсет, Валенвуд), тож «Висока Скеля» була єдиним винятком |
| Ancestor Moth | **міль предків** | так зветься істота в грі та інгредієнт «Крило молі предків»; у двох назвах книг був «Метелик Предків» |
| Peryite | **Периїт** | так у святині та трьох чарах; я був написав «Периайт» |
| Hircine | **Гірсін** | 9 записів; тут `H` дає **г**, на відміну від Хлаалу — виняток уже усталений |
| Herkel | **Геркель** | той самий виняток `H → Г`: «Херкель» в українському вусі звучить непристойно, а персонаж і без того весь час у смішному становищі |

### «Тридцять шість уроків Вівека»

Серія на 57 записів і ~105 тис. символів із наскрізною термінологією. Словник
закріплено тут, бо кожна проповідь тягне за собою ті самі імена й формули.

| Англійською | Українською | Чому саме так |
|---|---|---|
| 36 Lessons of Vivec | **36 уроків Вівека** | так на обкладинці в усіх 37 назвах; у тексті — «Тридцять шість уроків Вівека», щоб не було двох перекладів одного слова в одній книзі |
| Vehk / Ayem / Seht | **Вехк / Айєм / Сехт** | Вехк уже в темі «Оссуарій Вехка» |
| AYEM AE SEHTI AE VEHK | **АЙЄМ АЕ СЕХТІ АЕ ВЕХК** | у тексті сказано «семискладове» — сім складів збережено |
| ALMSIVI | **АЛЬМСІВІ** | 6 входжень великими літерами |
| The ending of the words is ALMSIVI | **Кінець слів — АЛЬМСІВІ.** | закриває кожну проповідь, тому формула стала |
| netchiman's wife | **дружина нетчаря** | «нетчар» від «нетч» так само, як вівчар від вівці |
| Clockwork King | **Механічний Король** | Сота Сіл; «годинниковий» звучало б про побутовий механізм |
| Face-Snaked Queen | **Змієлика Королева** | |
| Prince of Plots | **Князь Змов** | даедричний Prince у корпусі — «Князь» |
| Demiprince | **Напівкнязь** | те саме гніздо |
| Black Hands Mephala | **Мефала Чорні Руки** | Мефала вже усталена |
| Padhome / Boet-hi-Ah | **Падхоум / Боет-хі-Ах** | розщеплені форми Падомая і Боетії |
| Ehlnofey | **ельнофеї**, Старі Ельнофеї | народ — з малої, як данмери; земля й гілка — з великої |
| Hist | **Хіст** | `H → Х`; завжди з великої, бо інакше збіг із «хист» |
| Mer / Men | **мери / люди** | |
| Black Marsh | **Чорна Драговина** | |
| Deep Folk | **Глибинний Народ** | двемери очима кімерів |
| Triune | **Триєдиний** | АЛЬМСІВІ як троє в одному |

### Істоти (джерело істини — `items/creature_overrides.json`)

Bonewalker → **Костохід** · Bonelord → **Кістяний володар** · Hunger →
**Ненажера** · Clannfear → **Кланфір** · Daedroth → **Даедрот** · Dremora →
**Дремора** · Scamp → **Скамп** · Golden Saint → **Золотий святий** · Winged
Twilight → **Крилатий сутінок** · Silt Strider → **Силт-страйдер** · Mole Crab →
**Кротокраб** · Sload → **Слоад** · Dreugh → **дреуг** · Flame Atronach →
**полум'яний атронах** (не «вогняний»).

Blight → **моровий / морова хвороба**. Corprus → **корпрус**.

### Ремесла й вивіски

Armorer → **зброяр** · Smith → **коваль** · Enchanter → **зачарувальник** ·
Outfitter → **спорядник** · Pawnbroker → **лихвар / лихварка** · Apothecary →
**аптекар** · Bookseller → **книгар / книгарка** · Trader → **торговець /
торговка** · Alchemist → **алхімік**.

Формат вивіски: `Ім'я: професія` — «Ірґола: лихварка».

**Рід професії має збігатися зі статтю NPC.** На око це не ловиться: імен три
тисячі, і за багатьма стать не вгадаєш. Тому стать беремо з гри — у записі
`NPC_` є підзапис `FLAG`, біт `0x0001` означає жінку:

```
py tools\items\check_gender.py     # має бути «чоловічий рід для жінки: 0»
```

Так знайшлося 18 вивісок на кшталт «Бервен: торговець» (Бервен — жінка).
Перевірка потребує ігрових файлів, тож у CI не ходить — це локальна ревізія,
як `extract_items.py`. Додаєте вивіску з новою професією — впишіть її жіночу
форму у словник `FEMALE`, інакше перевірка мовчки її пропустить.

---

## 6. GMST — джерело істини для ігрових термінів

Гравець бачить ці слова на аркуші персонажа. **Вигадувати свої не можна**,
треба брати з `tools/gmst/gmst_en.json` (там уже українські значення).

| Термін | Українською | Пастка |
|---|---|---|
| Fatigue | **запас сил** | ✗ не «витривалість» — вона зайнята під Endurance |
| Endurance | витривалість | |
| Magicka / Spell Points | **мана** | |
| Health | здоров'я | |
| Drain | **Зменшення** | від «Зменшити запас сил» |
| Fortify | **Збільшення** | |
| Resist | **Стійкість до …** | |
| Weakness to | **Слабкість до …** | |

Навички: акробатика, алхімія, зміна, ковальська справа, атлетика, сокири,
володіння щитом, ударна зброя, чаклування, руйнування, зачарування, рукопашний
бій, важкі/легкі/середні обладунки, ілюзії, довгі/короткі мечі, влучність,
торгівля, містицизм, відновлення, обережність, скритність, списи, ораторство,
бій без обладунків.

Місяці (в назвах книг — у називному): Ранкова зоря, Схід сонця, Перший висів,
Рука дощу, Другий висів, Середина року, Високе сонце, Останній висів, Домашнє
вогнище, Осінні морози, Захід сонця, Вечірня зоря.

---

## 7. Відмінки в шаблонах

Український прийменник вимагає відмінка — англійський ні. Найчастіші шаблони:

| Шаблон | Українською | Відмінок |
|---|---|---|
| `Guide to X` | Путівник по **Балморі** | місцевий |
| `Map of X` | Мапа **Балмори** | родовий |
| `Note from/to X` | Записка від/до **Бервен** | родовий |
| `Shrine of St. X` | Святиня святого **Аралора** | родовий |
| `Wayshrine of X` | Придорожня святиня **Акатоша** | родовий |
| `Scroll of X` | Сувій **зцілення** | родовий |
| `Scroll of <Чарівник>'s <Ефект>` | Сувій **душогнилі Дратіса** | ефект + чарівник, обидва в родовому |
| `Summon X` | Виклик **кланфіра** | родовий |
| `Hand of X` / `Wrath of X` | Рука / Гнів **Дагота** | родовий |
| `X Legion Fort` | Форт легіону **Бакмот** | називний |
| `X Propylon` | Пропілон **Андасрет** | називний |
| `Tel X` | **Тель** Арун | |

### Рід

Прикметник узгоджується з іменником: `Надійн**ий** збалансований обладунок`,
`Надійн**а** прудкість`, `Надійн**е** зречення`. У композиторі це задано в
`items/lexicon.json` (`m/f/n/pl`).

**Святі Храму жіночого роду: Меріс, Рілмс, Серин** (а також обрано жіночий рід
для Нагаріне). Для них — «святої», «свята», «святій».

### Чотири тіри одного набору ефектів

`Assured` → **Надійний** · `Masterful` → **Майстерний** · `Sanguine` →
**Санґвінів** · `Surpassing` → **Незрівнянний**.

Самі ефекти (збалансований обладунок, глибокий вгриз, зречення, прудкість,
Рогатий Кулак, протинальний випад…) — **однакові в усіх чотирьох** і в речах
Санґвіна. «Assured» не «Певний» саме тому, що `Assured Sureflight` дало б
«Певний певний лет».

---

## 8. Велика літера

`patch_names.titled()` робить першу літеру великою вже під час збірки. Тож
у `uk_*.json` вона може бути будь-якою — у гру все одно піде з великої.
`q.py dup` це враховує й не сигналить про різницю в регістрі першої літери.

Причина існування цього: композитор складає назву з прикметника з лексикону
(з малої), а ручні переклади пишуться з великої. У грі це один список, і без
вирівнювання половина рядків виглядала б інакше за іншу. Зачеплено 3 776 назв.

---

## 9. Тексти книг

Живуть окремо від назв: `tools/books/`.

**Ключ — SHA-1 англійського тексту**, не порядковий номер. Причина: книги
приходять із 327 плагінів, чий склад і порядок у кожного гравця свій — нумерація
збилася б і переклад ліг би не на ту книгу. Побічна користь: у репозиторії не
лежить 8,5 МБ чужої прози.

| Файл | У git? | Що всередині |
|---|---|---|
| `uk_books.json` | так | хеш → український текст |
| `books_meta.json` | так | назва, довжина, підпис послідовності тегів, даедричні фрагменти |
| `_source.json` | **ні** | англійські тексти; відтворюється `extract_books.py` з копії гри |

### Правила

1. **Розмітку лишати байт у байт**: `<BR>`, `<DIV ALIGN="…">`, `<FONT …>`,
   `<IMG SRC="…">`. Кількість і порядок тегів мусять збігтися з оригіналом —
   `check_sources.py` це перевіряє за підписом.
2. **`FACE="Daedric"` не перекладати.** Цей шрифт малює **латинські** літери як
   даедричні руни; кирилиця в ньому просто не намалюється. Таких фрагментів
   у 123 книгах, здебільшого в сувоях.
3. Описки оригіналу (`Guide to BlacklightBR>` без відкривної дужки) **лишати**:
   інакше кількість тегів розійдеться й перевірка відкине переклад.
4. Квадратні дужки `[Це поховальні записи…]` — не текст книги, а опис того, що
   бачить гравець. Лишаються в дужках.
5. 152 книги **без читаного тексту** (самі руни, картинка або порожня
   заготовка) видобувач відсіює сам — вони не висять у роботі. Серед них
   ~108 сувоїв, у яких геть увесь напис даедричний. **Перш ніж вирішувати, що
   якась книга «застрягла», подивіться на `notext` у `books_meta.json`** —
   я одного разу вже був зібрався вписати ці 108 сувоїв у `uk_books.json`
   англійськими, хоч вони давно поза роботою. `check_sources.py` тепер друкує
   обидві цифри: скільки з 2 134 у роботі й скільки записів у файлі взагалі.
6. Ще 41 книга — **нотатки для модерів**: `InfoBox — …` з `OAAB_Data`
   і `Tamriel_Data`, `<Deprecated>`, `<Placeholder>`. Гравець їх не бачить
   (на обкладинці стояло б «InfoBox»), тож видобувач позначає їх `devnote`
   і теж прибирає з черги.
7. **Шифри.** Вирішує те, чи можна прочитати відкритий текст:
   * нісенітниця без ключа (`KJAH SDFH KLJH…` у «Treasury Report»,
     «Sottilde's Code Book») — записуємо в `uk_books.json` **як є**. Інакше
     книга вічно висітиме в черзі неперекладених;
   * розгадуваний шифр («Neminda's Orders» — це атбаш, `SOZZOF` = `HLAALU`)
     — розгадуємо, перекладаємо відкритий текст і **зашифровуємо наново тим
     самим способом, але вже по українській абетці**. Інакше українець,
     розгадавши шифр, побачив би англійську. Код перетворення лишається
     в скрипті партії разом із відкритим текстом.

   Перш ніж оголосити шифр безключовим, **перевір регістр і напрям читання**.
   Блок у «Проповіді Нуль» виглядає як суцільна нісенітниця, але малі літери
   серед великих, читані задом наперед, дають «to the dwemer and oblivion
   belong this treasure and they are there dead». Кирилиця має ті самі два
   регістри, тож спосіб переноситься дослівно; блок згенеровано
   `scratchpad/cipher0_uk.py` із закріпленим зерном, щоб відтворювався
   байт у байт.

### Книги з густою розміткою: міняти прозу в оригіналі, а не набирати теги

Звичайний спосіб — скласти український текст разом із розміткою — ламається на
книжках, де тегів багато і вони різні. У «Периїтоні» шість `<IMG
SRC="TR\peryiton_*.dds" …>`, а `<BR>` і `<br>` стоять упереміш; підпис
послідовності **чутливий до регістру**, тож набране з голови рано чи пізно
поставить `<BR>` там, де в оригіналі `<br>`.

Для таких книжок беремо англійський текст із `_source.json` і замінюємо в ньому
**самі фрагменти прози**, по одному:

```python
def retext(src, pairs):
    out = src
    for en, uk in pairs:
        if en not in out:
            raise SystemExit('фрагмент не знайдено: %r' % en[:70])   # падаємо, а не пишемо тишком не те
        out = out.replace(en, uk, 1)
    left = re.findall(r'[A-Za-z]{2,}', TAG.sub('', out))             # що лишилося поза тегами
    ...
```

Розмітка при цьому не змінюється взагалі, тож підпис збігається завжди, а
перевірка на латиницю поза тегами ловить пропущений абзац.

### Та сама книга під іншою назвою

`propagate.py` і черга «найбільша група першою» дивляться на **назву**. Через це
ціла родина копій була невидима: «Odral's History of the Empire 2» і «Brief
History of the Empire v 2» — один текст із двох плагінів під двома обкладинками.

```
py tools\books\same_text.py     # групує за текстом, а не за назвою
```

Перш ніж рахувати книгу самотньою, спитайте цей скрипт. Уся серія «Коротка
історія Імперії» виявилася такою: по чотири записи на том замість двох.

### Близнюки

Та сама книга приходить із кількох плагінів і щоразу трохи інакша — десь
`\r\n` замість `\n`, десь зайва кома. Хеш через це різний, і книга висить
у черзі двічі-тричі. Виміряно: **296 назв, 3,7 млн символів** — майже
половина решти.

```
py tools\books\propagate.py --near     # хто на кого чекає і чим різниться
py tools\books\propagate.py --apply    # перенести переклад на близнюків
```

Гравець бачить не байти, а зверстану сторінку, і CI перевіряє **послідовність
тегів**, а не пробіли. Тому перенести можна, коли підпис тегів той самий і
англійські тексти збігаються після згортання пробілів (рівень 1) або ще й після
викидання розділових знаків (рівень 2 — українську пунктуацію ми ставимо свою).
Різниця бодай в одному **слові** переносу не дозволяє: це інша редакція.

Практичний висновок: **перекладати варто найбільшу групу, а не найкоротшу
книгу.** Переклали одну копію — запустіть `propagate --apply`, і решта групи
закриється сама або звузиться до правки в одне слово.

### Порядок роботи

Розподіл довжин дуже нерівний, тож найкоротші першими:

| Довжина | Книг | Символів |
|---|---:|---:|
| до 1 000 | 746 (32%) | 269 тис. (3%) |
| 1 000–5 000 | 978 | 2,57 млн |
| понад 5 000 | 595 | 5,7 млн (67%) |

Третина всіх книг — за 3% зусиль, і це саме записки, вивіски й підказки, які
трапляються гравцеві найчастіше.

---

## 10. Цикл роботи над партією

1. `py tools\q.py left <категорія> 150` — узяти наступний шматок
2. `py tools\q.py key <термін>` — **звірити терміни, що вже вживалися**
3. написати партію окремим скриптом у scratchpad (не `py -c`: лапки з апострофами
   ламають bash), який оновлює `<cat>_overrides.json`
4. `py tools\loc.py` — валідація й збірка
5. `py tools\q.py dup` — **має бути 0**
6. `py tools\items\check_generated.py` — **чи перезбірка нічого не з'їла**:
   складені `uk_*.json` мусять збігатися з git, окрім свідомих правок цієї партії
7. `py tools\dialogue\latin_left.py --all` — англійська всередині перекладу
8. оновити рядок у `PROGRESS.md`
9. написати `tools\_msg.txt` і `py tools\loc.py save`

Крок 2 — найважливіший. Майже всі колізії виникли там, де я перекладав наново
те, що вже мало назву в іншій категорії.

---

## 11. Пастки, на які вже наступали

- **`translit.py` не був у списку композиторів `loc.py`** — правки імен NPC
  лежали в overrides і нікуди не доїжджали. Якщо додаєте новий композитор,
  впишіть його в `loc.py`.
- **Однакові назви в різних категоріях.** Чари `Bound Boots` і обладунок
  `Bound Boots` — це один рядок для `patch_names`.
- **Ефект, названий двічі.** `Flame Atronach` був і «полум'яним», і «вогняним» —
  різні англійські ключі, тож `q.py dup` цього не бачив. Звіряйте `q.py key`.
- **Fatigue ≠ Endurance.** Обидва хотілося назвати «витривалістю».
- **Назва предмета всередині перекладеної репліки.** Рядок вважають готовим,
  щойно він з'явився в `uk/` — і англійська назва посеред українського речення
  лишається непоміченою, бо око читає речення як ціле. У грі це гірше за
  переклад узагалі: у журналі «Ring of Sanguine Fluid Evasion», а в інвентарі
  той самий перстень уже українською, і завдання не пройти. `q.py dup` цього
  не бачить (рядок один, переклад один) — шукає `dialogue\latin_left.py`.
- **Закладки модера серед реплік.** Частина `INFO` — не репліки, а мітки, які
  автор мода лишив собі в редакторі: `**** IF ОДЯГ ****`, `FMI - LEGION, RAW
  EBONY------`, `PUBLICANS`, `KHAJIIT MALE`, `Crossbow`, `Goblet10`,
  `FMIHandinEBONY = 1`. Їх не перекладаємо: гравець їх не бачить, а «IF» там —
  код фракції, не англійське слово. Через це зріз `mod_fmi_servicerefusal`
  ніколи не покаже 100%: 108 його рядків саме такі.
- **`@фраза#` зі старого перекладу.** Легасі-репліки несли розмітку на
  англійську тему (`@Wraithguard#`), якої в нашому списку тем немає, тож
  посилання вело в нікуди, а `mark_topics.py` такі рядки навмисно обходить.
  Шукає `topics\validate_topics.py` рядком «unknown topic».
- **Родина назв ламається по одній.** `Ash Slave`, `Ash Ghoul` і `Ash Statue`
  були «попелясті», а `Ash Vampire`, `Ash Storm` і `Ash Yam` у тексті —
  «попелові». Жодної пари з однаковим англійським ключем, тож `dup` мовчав.
  Коли правите одне ім'я з родини, перевірте всю родину.
- **`-bolt`.** У чарах — «стріла» (Блискавична стріла), але `Flamebolt`
  і `Viperbolt` існують ще й як **арбалетні болти**, і там «болт» точний, тож
  чари підлаштовано під предмет.
- **`Scales`** — і луска (інгредієнт), і терези (начиння). Словник спільний,
  обрано «Луска». Такі омоніми нерозв'язні, обирайте частіший сенс і запишіть.
- **Службові заготовки** `<Deprecated>`, `<Placeholder>`, `<Template …>`
  лишаємо неперекладеними: гравець їх не побачить.
- **Описки в оригіналі** (`Saguine Leaping`, `Grave Curse: Agilitiy`,
  `Rilm's`/`Rilms'`, `Wulfharth's`/`Wulfharth’s`) — це **окремі записи в грі**.
  Перекладати обидва, однаково.
- **Лапки-ялинки.** `« »` і `„` проходять перевірку cp1251 і все одно не
  показуються — гліфа в шрифті нема. Ловилося лише очима, аж поки не з'явився
  `glyphs.py`. Робоча пара — `“ ”`.
- **Заміна лапок посимвольно.** Перший прохід замінив `„`→`“` окремо від
  закривної, і з `„текст“` вийшло `“текст“` — дві відкривні. `glyphs.fix`
  тепер обробляє **пару цілком**, і тільки потім поодинокі символи.
- **`_source.json` не чіпати.** Хеші книг рахуються саме з нього; будь-яка
  «косметична» правка англійського тексту відв'яже всі переклади розділу.
- **`’` не ламає нічого.** У цьому файлі колись стояло, що U+2019 немає
  в cp1251. Неправда: він там 0x92, і гліф у шрифті теж є. Заборонений він
  лише заради одноманітності, і саме тому потрібна окрема перевірка — інакше
  він проходить непоміченим.
- **Відповіді задом наперед.** У «Жовтій книзі загадок» відповідь надрукована
  перевернутою (`dael :rewsnA ehT`), щоб не впала в око. Українську теж
  перевертаємо — і саме рядок, а не англійський.
- **Початкова `и`.** Транслітератор давав `Yngling → Инґлінґ`: правило `y→и`
  спрацьовувало й на початку слова. Українське слово з «и» не починається —
  тепер початкове `y` дає **і**.
      
- **Обкладинка й текст — різні записи.** Назва книги живе у
  `book_title_overrides.json`, текст — у `uk_books.json`, і `q.py dup` їх не
  зіставляє. Я написав усередині «Тридцять шість проповідей Вівека», тоді як на
  обкладинці вже 37 разів стояло «36 уроків Вівека» — одне англійське слово,
  два переклади в одній книзі. **Перед тим як писати заголовок усередині
  тексту, звірте `q.py key <назва>`.** Обкладинка старша: гравець шукає по ній.
- **Словник предметів — це не весь корпус.** `q.py key` показує самі лише назви
  речей, а імена, що живуть у діалогах і щоденнику, він мовчки проминає. Саме
  так у §5 з'явився «Молаґ Бал» (5 записів у предметах проти **36** «Молаг Бал»
  у діалогах), і саме так я вигадав «Дім Напастей», коли в щоденнику вже дев'ять
  разів стояв «Дім Бід». Так само я писав «Даґон», хоч у корпусі 123 «Дагон».
  Перед новим іменем питайте **обидва**: `q.py key` і `q.py val`.
- **Правка у складеному словнику не доживе до наступної збірки.** `uk_npc.json`,
  `uk_creature.json` і решта `uk_*.json` **складаються** композиторами, і
  `loc.py rebuild` запускає їх щоразу. Я виправляв імена просто в них — і
  перезбірка мовчки повертала машинний варіант: «Джентльмен Джим Стейсі» знову
  ставав «Гентлеман Джім Стацеі», «Дядько Солодкодар» — «Ункле Світшаре», уся
  родина Ash-* — «попелястою». Так пропало **116 імен і 9 предметів**, і
  побачив це один-єдиний запис, що зачепився за тему діалогу в `dup`. Правка
  живе в `*_overrides.json` — і ніде більше. Перевірити, чи нічого не
  загубилося, можна порівнявши версію з git зі свіжою перезбіркою.
- **Правило, записане лише в документі, не діє.** §4 постановив `g → г` ще
  минулої партії, але `translit.py` і далі давав ґ, тож кожна перезбірка тягла
  словник назад. Ухвалюючи правило про транслітерацію, міняйте **таблицю в
  інструменті**, інакше документ і збірка розходяться, і виграє збірка.
- **Українська в англійському зрізі.** Проєкт увібрав давній переклад базової
  гри (`absorb_base.py`), і 57 його реплік потрапили в `src/` як **джерело**, а
  19 назв тем — у `dial_topics.json`. Виглядає, ніби рядок ще не перекладено;
  насправді він неперекладний узагалі, бо ключ пам'яті — сам текст джерела, а
  такого тексту в первісних плагінах немає. У грі гравець бачив чужий стиль
  («ви» замість «ти»), і полічити це було нічим. Англійську можна відновити за
  `INAM`, id запису INFO: він у `corpus.json` і в самих плагінах.
- **Закам'янілий шлях вимикає перевірку мовчки.** `audit.py` і
  `extract_infos.py` берегли шлях до каталогу мода рядком. Каталог
  перейменували — і перевірка «не рахувати власну латку» перестала збігатися,
  тож `audit.py` міряв **наш український текст замість англійського джерела**.
  Помилки не було, просто цифри поїхали. Шлях беремо від файлу через
  `paths.py`, ніколи рядком.
- **`make_slices.py` перезаписує `src/topics01..10.json`.** Він бере все, чого
  ще нема в зрізах, і ділить на десять файлів **під тими самими іменами**.
  Переклад прив'язаний до **номера рядка**, тож після такого запуску всі
  `uk/topics01_p*.json` вказували б на інші репліки. Нові зрізи пише
  `tools/dialogue/extract_infos.py` — і тільки під новими іменами `mod_*`,
  а перед записом звіряє, що новий список починається рівно зі старого.
- **Регістр у перенесенні реплік.** `reuse.py` знеособлює пунктуацію й регістр,
  бо фікси правлять саме їх (`ashlands` → `Ashlands`). Через це `NORD` збігся
  з `Nord?` і дістав «Норде?» — з питальним знаком, якого в оригіналі нема.
  У `FMI_ServiceRefusal_Contraband` такі слова великими (NORD, REDGUARD,
  KHAJIIT) — позначки, а не мовлення. Тепер «весь рядок великими» входить
  у ключ звіряння.

---

## 12. Що заблоковано і чому

**Назви місць (1 647).** `CELL NAME` — це водночас видима назва **й**
ідентифікатор, на який посилаються скрипти (`AddTopic`, телепорти) і двері.
Перейменування зламає переходи. Розблокувати можна лише так само, як зробили
з темами діалогів: перейменувати запис **і одночасно** переписати всі посилання
в `SCTX` скриптів. Прецедент є — `tools/topics/rename_topics.py`.
