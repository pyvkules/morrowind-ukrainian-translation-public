# Українізація Morrowind — прогрес і план

> Живий документ. Оновлюється щосесії. Цифри — **виміряні**, не оцінені:
> перерахувати можна будь-коли командою `py tools\audit.py`.
>
> Останнє оновлення: **2026-07-23**

---

## Стисло

Перекладається **весь модліст** `just-good-morrowind-plus` (403 плагіни), а не лише
базова гра. Мета — **100% українською**, без невиправданих англійських залишків.

| | Унікальних рядків | Символів |
|---|---:|---:|
| **Усього в модлисті** | **59 514** | **13 756 699** |
| Перекладено | ~18 900 | ~2 370 000 |
| **Готовність** | **~32%** | **~17%** |

Символів набагато більше, ніж рядків, бо **книги — це 8,47 млн символів (62% усього обсягу)**.

---

## Стан за категоріями

| Категорія | Готово | Усього | % | Пріоритет |
|---|---:|---:|---:|---|
| Репліки діалогів (INFO) | 17 240 | 33 913 | 51% | 🔴 1 |
| Теми діалогів (DIAL) | **2 333** | 2 333 | **100%** | ✅ |
| Тексти книг | 0 | 2 305 | 0% | 🟡 4 |
| Назви книг | 0 | 2 074 | 0% | 🟢 3 |
| Імена NPC | **3 242** | 3 242 | **100%** | ✅ |
| Обладунки | 2 113 | 2 113 | 100% | ✅ |
| Зброя | 1 900 | 1 900 | 100% | ✅ |
| Назви чарів | 37 | 1 744 | 2% | 🟢 2 |
| Істоти | 771 | 774 | 100% | ✅ (3 шаблони-плейсхолдери пропущено) |
| Назви місць (комірок) | 0 | 1 647 | 0% | 🟡 3 |
| Різні предмети | 1 619 | 1 619 | 100% | ✅ |
| Активатори | 0 | 1 297 | 0% | 🟢 2 |
| **Інтерфейс (GMST)** | **1 060** | 1 060 | **100%** | ✅ |
| Одяг | 904 | 904 | 100% | ✅ |
| Контейнери (ящики) | 740 | 742 | 100% | ✅ (2 <Deprecated> пропущено) |
| Інгредієнти | 712 | 712 | 100% | ✅ |
| Зілля | **538** | 538 | **100%** | ✅ |
| Двері | **155** | 155 | **100%** | ✅ |
| Світильники | **55** | 55 | **100%** | ✅ |
| Апаратура, ремонт, відмички, зонди | **78** | 78 | **100%** | ✅ |
| Класи, раси, знаки, фракції, регіони | частково | 326 | ? | 🟢 2 |

**Окремо (не в ESM, вже зроблено):**

- ✅ Інтерфейс рушія OpenMW — 441 ключ, 8 контекстів (`l10n/*/uk.yaml`)
- ✅ Лаунчер і майстер налаштування — 2 053 рядки (`.qm`)
- ✅ Шрифти — Pelagiad пропатчено (і ї є ґ); MysticCards був марний, гра його не вантажить

---

## Порядок робіт

### ✅ Зроблено: теми діалогів (2 333 / 2 333)

Виміряний підсумок після перейменування по всьому модлисту:

| | |
|---|---:|
| перейменовано записів `DIAL` у 59 плагінах | 3 770 |
| переписано аргументів `AddTopic` у скриптах | 5 702 |
| рядків у `morrowind.top` (форми + англійські назви) | 4 689 |
| тем, що лишилися латиницею | **0** |
| «висячих» `AddTopic` (у оригіналі було 20) | **20** |
| помилок `esmtool` на 68 переписаних плагінах | **0** |

Ключове відкриття: OpenMW показує в панелі тем **сам ідентифікатор запису `DIAL`**,
тому тему видно українською лише якщо перейменовано сам запис. А ідентифікатор — це
те саме, що скрипти передають у `AddTopic`, тож перейменування і правка тексту
скриптів мусять відбуватися **одночасно**. Скомпільований байт-код (`SCDT`) не
чіпаємо: OpenMW перекомпільовує з `SCTX` і ігнорує компіляцію Bethesda — саме це
робить перейменування безпечним.

Відмінки розв'язано автоматично — див. «Механізми» нижче.

### ✅ Зроблено: інтерфейс (GMST, 1 060 / 1 060)

Виявилося, що 748 налаштувань уже були українською з давнього перекладу; ми
доперекладали 116 і виправили хибний `sMagicContractDisease` («Ви найняли %s»
замість «Ви підхопили %s»). GMST може перевизначити будь-який пізніший плагін,
тому STRV переписано в **усіх 6 плагінах**, що їх визначають, а не лише в
`Morrowind.esm` — інакше Tribunal і Bloodmoon повернули б англійську.

**167 налаштувань свідомо лишено латиницею** — це єдиний технічно виправданий
виняток у грі, перелік і причини у `tools/gmst/gmst_keep.json`:

- `sKeyName_*` — підписи фізичних клавіш; мусять збігатися з тим, що надруковано на клавіші
- `sMagic*ID` — це **ідентифікатори записів**, за якими рушій створює викликаних істот
  і зв'язані предмети; переклад зламав би самі закляття
- `sXTimes`, `sLevelUpMenu3/4` — знак множення `x`, `x2`, `x3`
- `sQuick0Xbox`…`sQuick9Xbox` — мертві заглушки, значення яких дорівнює власному ключу

Перевірка «чи не лишилося невиправданих англійських рядків» відтворювана:
непокриті кейпом англійські GMST мусять давати **0**.

### 🟢 Зараз: назви предметів (~15 000 рядків, ~310 тис. символів)

**Зроблено:**

- **Зброя/обладунок/одяг — 1 184** через складальний двигун `compose_names.py`:
  назва = [прикметник(и)] + тип, прикметник узгоджено в роді/числі з типом
  (`сталева кіраса`, `залізні чоботи`, `викликаний кинджал`). Лише повністю
  розібрані назви; чари «of X» і власні назви — наступний прохід.
- **Інструменти й світло повністю вручну:** апаратура 41, ремонт 21, відмички 9,
  зонди 7, світильники 54 (маркер редактора лишено).

**Далі:** зілля (538, дуже регулярні «[якість] Зілля X»), інгредієнти (712),
різні предмети (1 619), чари (1 697), NPC/істоти (транслітерація),
контейнери/двері. Розширювати словник `lexicon.json` і покривати чари «of X».

### 🟡 Далі: назви місць і книг

Місця (1 647) чіпають карту й швидку подорож — треба звірити з назвами тем.
Назви книг (2 074) — короткі.

### 🟡 Найбільше: репліки модів (16 673 рядки, ~2,6 млн символів)

Tribunal, Bloodmoon, Tamriel_Data, діалогові моди.
Виміряно: лише ~10% можна перенести з наявного перекладу, решту треба перекладати.

### 🔵 Найдовше: тексти книг (2 305 / 8,47 млн символів)

62% усього обсягу. Потребує розширення `rebuild_esm.py` на записи `BOOK`
зі збереженням розмітки `<FONT>`, `<DIV>`, `<BR>`.

---

## Механізми (щоб не винаходити заново)

| Скрипт | Що робить |
|---|---|
| `tools/rebuild_esm.py` | збирає `Morrowind.esm` з `base.esm` + `src/` + `uk/` |
| `tools/patch_plugins.py` | переносить наявний переклад у **решту плагінів** (`--apply`) |
| `tools/audit.py` | перераховує всі категорії — джерело цифр цього файлу |
| `tools/fuzzy_scan.py` | скільки модового тексту можна перевикористати |
| `tools/topics/harvest_forms.py` | збирає відмінкові форми тем з нашого корпусу (для mark_topics) |
| `tools/topics/mark_topics.py` | **розмічає `@форма#` + пише `Morrowind.top`** для клікабельних тем (`--apply`) |
| `tools/topics/rename_topics.py` | перейменовує `DIAL` і править `AddTopic` у всіх плагінах (`--apply`) |
| `tools/topics/validate_topics.py` | перевіряє цілісність тем (`--baseline` — порівняти з оригіналом) |
| `tools/gmst/extract_gmst.py` | збирає всі рядкові GMST з модліста |
| `tools/gmst/patch_gmst.py` | пише український GMST у майстри `.esm` (`--apply`) |
| `tools/items/compose_names.py` | **складає назви предметів** із словника з узгодженням роду |
| `tools/items/translit.py` | транслітерує імена NPC (звучання як в оригіналі; -ius за римською традицією; епітети перекладає); ручні винятки в `npc_overrides.json` |
| `tools/patch_names.py` | пише перекладені назви (FNAM) у всі плагіни (`--apply`) |
| `tools/patch_font.py` | додає українські гліфи у шрифт |
| `tools/build_top.py` | збирає `morrowind.top` |

### Чому переклад «зникає» в грі

Плагіни вантажаться **після** `Morrowind.esm` і підсовують ту саму англійську назад.
Тому будь-який новий переклад треба **щоразу** проганяти через `patch_plugins.py --apply`.

### Порядок запуску має значення

`patch_plugins.py` читає **оригінали** з каталогів модів, а `rename_topics.py` —
наші копії з кореня мода. Тому лише в такому порядку:

```
py tools\rebuild_esm.py
py tools\patch_plugins.py --apply
py tools\topics\harvest_forms.py
py tools\topics\rename_topics.py --apply
py tools\gmst\patch_gmst.py --apply
py tools\topics\harvest_forms.py
py tools\topics\mark_topics.py --apply
py tools\topics\validate_topics.py
```

`mark_topics.py` мусить іти **останнім** із тих, що переписують текст реплік: він
розмічає `@…#` і дописує запасні `AddTopic` у вже перекладені копії, тож будь-який
пізніший крок стер би ці зміни. Він також пише `Morrowind.top`, який рушій читає при
старті — файл мусить лежати в data-каталозі (наш корінь мода).

Запустити `patch_plugins.py` після `rename_topics.py` — означає стерти перейменування.
Копія перед перейменуванням лежить у `_backup-before-topic-rename\`.

**Побічний ефект:** корінь мода тепер затіняє 68 плагінів. Якщо колись оновлюється
якийсь мод — його треба прогнати через цей конвеєр наново, інакше гра бачитиме нашу
стару копію.

### Як розв'язано відмінки (правильне рішення — 2026-07-25)

Рушій робить слово клікабельним за **дослівним префіксним** збігом з ідентифікатором
теми. Українська відмінює, тож «про Балмор**у**» не збігається з темою «Балмор**а**»
(працює лише для чоловічих на приголосний: легіон→легіону). Виміряно: губилося
**~75%** клікабельних згадок.

**Ключове відкриття (і виправлення моєї хибної гіпотези):** OpenMW **читає**
`Morrowind.top` — я помилково стверджував протилежне (у виконуваному файлі немає
рядка «morrowind.top», бо ім'я збирається з основи ESM у рантаймі). Механізм у
`components/translation/translation.cpp` + `engine.cpp` (`loadTranslationData`):

- при старті вантажить `<ESM>.top` (форма→тема), `.cel` (назви комірок), `.mrk`;
- `parseHyperText` перетворює розмітку `@фраза#` на посилання: показує фразу, а веде
  на тему, яку `topicStandardForm` бере з `Morrowind.top`.

Тобто **суто дані, без патчу рушія** — так робили рос./пол. локалізації.

**Розв'язання:** `tools/topics/mark_topics.py`
1. обгортає відмінкові згадки тем у `@форма#`;
2. пише `Morrowind.top` (форма→тема).
Обгортає **лише там, де англійський оригінал репліки посилався на цю тему** — щоб
спільні слова («знак», «допомогти») не ставали хибними посиланнями. Де згадку не
знайдено (переклад перефразував) — тему додає запасним `AddTopic`.
Результат: **34 тис. згадок** розмічено, `Morrowind.top` ~2,5 тис. рядків.

---

## Правила перекладу

- Назви тем **мусять збігатися** з тим, як слово вже перекладено в тексті — інакше пошук не спрацює
- Дві теми не можуть мати однакову назву (зіллються в одну)
- Тільки кирилиця + ASCII; латинські двійники (é, ú) і апостроф `ʼ` ламають cp1251 — лише `'`
- `%PCName`, `%Race`, `%Faction`, `\r\n` — зберігати дослівно
- Уникати «» — їх немає в шрифті

---

## Перевірка після кожної партії

```
py tools\rebuild_esm.py            # має бути: encode warnings: 0
py tools\patch_plugins.py --apply  # перенести в решту плагінів
openmw-validator.exe --cfg <профіль>\openmw.cfg --stdout
```

Два попередження валідатора про `LoadingScreensDiversified` і `DunmerLanternsReplacer` —
давні й не стосуються перекладу.
